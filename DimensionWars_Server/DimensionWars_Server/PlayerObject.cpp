#include "stdafx.h"
#include "PlayerObject.h"


PlayerObject::PlayerObject()
{
	
}


PlayerObject::~PlayerObject()
{
}

void PlayerObject::Move(unsigned char dir)
{
	//if (dir) {
	//	
	//	if (dir & DIR_FORWARD)
	//	{
	//		xmf3Shift = Vector3::Add(xmf3Shift, m_Look, +fDistance);
	//	}
	//	if (dir & DIR_BACKWARD)
	//	{
	//		xmf3Shift = Vector3::Add(xmf3Shift, m_Look, -fDistance);
	//	}
	//	if (dir & DIR_RIGHT)
	//	{
	//		xmf3Shift = Vector3::Add(xmf3Shift, m_Right, +fDistance);
	//	}
	//	if (dir & DIR_LEFT)
	//	{
	//		xmf3Shift = Vector3::Add(xmf3Shift, m_Right, -fDistance);
	//	}

	//	if (dir & DIR_UP&&jump_check == false)
	//	{
	//		//xmf3Shift = Vector3::Add(xmf3Shift, packet->m_Up, +fDistance * 2);

	//		if (character_type == 0)
	//			xmf3Shift = Vector3::Add(xmf3Shift, m_Up, +fDistance * 2);
	//		else if (character_type == 1)
	//		{
	//			//jump_check == true;
	//			xmf3Shift = Vector3::Add(xmf3Shift, m_Up, 200);

	//		}
	//	}
	//	if (dir & DIR_DOWN)
	//	{
	//		xmf3Shift = Vector3::Add(xmf3Shift, m_Up, -fDistance);

	//	}

	//}
}